# YT-Playlist-Scraper

An user script for scraping playlist information from Youtube.

## Features

This user script iteratively tries to scrape the playlist information until success. It then outputs it as a `JSON` string both to the console and as a click-to-copy button on the bottom-right corner.
